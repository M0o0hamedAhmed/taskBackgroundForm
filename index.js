let colors = ['blue', 'red'];
gradient = `linear-gradient(to right ,${colors[0]} , ${colors[1]})`;
document.body.style.backgroundImage = gradient;
document.getElementById('form').style.minHeight = '500px'